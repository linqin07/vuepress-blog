language: node_js
node_js:
  - lts/*
install:
   npm install -g vuepress
script:
  - chmod u+x deploy.sh
  - ./deploy.sh
deploy:
  provider: pages
  skip-cleanup: true
  local_dir: docs/.vuepress/dist
  github-token: $GITHUB-TOKEN # b8ccb24b96ab91453040ef376fd6438abbc21549
  keep-history: true
  on:
    branch: gh-pages